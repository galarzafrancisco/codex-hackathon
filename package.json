{
  "name": "codex-symphony-demo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/backend",
    "dev:1": "dotenv -e stack1.env -- npm run dev -w apps/backend",
    "dev:2": "dotenv -e stack2.env -- npm run dev -w apps/backend",
    "dev:3": "dotenv -e stack3.env -- npm run dev -w apps/backend",
    "dev:4": "dotenv -e stack4.env -- npm run dev -w apps/backend",
    "dev:5": "dotenv -e stack5.env -- npm run dev -w apps/backend",
    "start:dev": "npm run dev",
    "build": "npm run build --workspaces --if-present",
    "openapi": "npm run generate:openapi -w apps/backend",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5.9.3"
  }
}
